name: SortOrder
in: query
description: Sort Order parameter
required: false
schema:
   description: Sort columns orders
   type: array
   items:
      type: string
      enum:
      - ASC
      - DESCC